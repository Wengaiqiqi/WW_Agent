__pycache__/
*.pyc
.pytest_cache/

# Web JWT signing secret persisted by 邀请码开关.bat — never commit (leaking it
# lets anyone forge login tokens for any user).
.web_auth_secret
htmlcov/
.coverage
.agent/runtime/
.agent/logs/
.claude/
projects/
!projects/README.md
temp/
*.egg-info/
.venv/
dist/
build/
.DS_Store
Thumbs.db
.idea/
.vscode/
.langchain-agent/

# Local-only diagnostic + scratch dirs that have caused noisy git status.
# .tmp/ holds dev throwaway scripts (diag_*, time_*, verify_*) — keep them
# locally but never commit. agent/ at the root has been accidentally typed
# multiple times where agents/ was intended; ignoring stops the stray subdir
# from showing up in `git status` again. *.docx at the repo root catches
# stray Word docs uploaded for ad-hoc testing — fixtures should live under
# tests/fixtures/ and skill assets under skills/.
.tmp/
/agent/
/*.docx
/yanghui_triangle.py

# Scratch and legacy code kept locally for reference but not part of the
# shipped surface. ``try/`` is a sandbox for one-off experiments; ``legacy/``
# preserves the pre-multi-agent in-process loop for comparison. Both have
# been showing up in `git status` noise — gitignore them and surface them
# explicitly when someone actually intends to commit.
/try/
/legacy/
/.mypy_cache/

# Local tooling / scratch output that kept showing up as untracked noise.
.gstack/
/7766.txt
*temp_wt.txt
