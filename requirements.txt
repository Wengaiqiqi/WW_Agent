# Flat install list. Source of truth for dependency *groups* is pyproject.toml;
# this file mirrors its [project.dependencies] + [project.optional-dependencies.dev]
# for environments that prefer `pip install -r requirements.txt`.

# Core dependencies
langchain>=0.3.0
langchain-core>=0.3.0       # imported directly (agent_loop, tools, tool_moa, …)
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0
rich>=13.0.0
prompt_toolkit>=3.0.0
pydantic>=2.0               # planner decision validation (orchestrator/turns.py, web/app.py)

# Multi-agent refactor dependencies
mcp>=1.0.0
a2a-sdk>=0.2.0              # declared for A2A interop; protocol currently hand-rolled on starlette
pyjwt>=2.8.0
httpx>=0.27.0
fastapi>=0.110.0
starlette>=0.37.0          # imported directly by a2a_protocol / a2a_server (not just via fastapi)
uvicorn>=0.29.0
psutil>=5.9.0

# Chat-platform gateway dependencies (gateway/feishu.py, gateway/qq.py)
cryptography>=42.0.0        # Feishu encrypt-mode AES-256-CBC decryption
websockets>=12.0           # QQ Official Bot gateway client
lark-oapi>=1.4.0           # Feishu long-connection (ws) adapter (gateway/feishu_ws.py)
requests>=2.32.5           # provider detection in config/_settings.py

# Development dependencies (mirror of pyproject [project.optional-dependencies.dev])
pytest>=7.0.0
pytest-asyncio>=0.23
pytest-cov>=4.0.0
pytest-mock>=3.10.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
trustme>=1.0.0
bandit>=1.7.0
pip-audit>=2.7.0
